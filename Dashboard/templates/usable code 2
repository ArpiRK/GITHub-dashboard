<!--
      <h1>Repository Details {{ username }}</h1>
    {% if repositories %}
    <ul class="scroll-list">
      {% for repo in repositories %}
      <li>{{ repo }}</li>
      {% endfor %}
    </ul>
    {% else %}
    <p>No repositories available.</p>
    {% endif %} 
  -->
  <!--
      <p><strong>Public repositories:</strong>
      {% if user_details.repositories %}
        <ul class="scroll-list">
          {% for repo in user_details.repositories %}
            <li>{{ repo.name }}</li>
          {% endfor %}
        </ul>
        {% if user_details.repositories|length > 0 %}
        <h2>Charts</h2>
        <div style="display: flex; justify-content: space-around">
          <div>
            <h3>Line Chart</h3>
            <img src="data:image/png;base64,{{ get_line_chart(user_details.repositories[0].name) }}" alt="Line Chart" />
          </div>
          
          <div>
            <h3>Bar Chart</h3>
            <img src="data:image/png;base64,{{ get_bar_chart(user_details.repositories[0].name) }}" alt="Bar Chart" />
          </div>
        </div>
        {% endif %}

      {% else %}
        No public repositories available.
      {% endif %}
    </p>
  -->

  